export interface ISample {
    id: string,
    sampleCode: string,
    client: string,
    getSampleDate: string,
    receptionDate: string,
    analysisDate: string,
    sampleLocation: string,
    responsable: string
}
